# Optional elevated specs

Scratch for production-elevated task notes: `MemoryBank/coord/specs/<TASK_ID>.md`.

- Not SSOT — `CURRENT.md` remains the session pointer.
- Never create `MemoryBank/active/`.
- Not boot-mandatory — see `../EXECUTION_GATES.md`.
- Acceptance criteria should name commands that must exit 0 (`bin/avcoord gate`).
