# Cursor overrides (Composer only)

- Prefer multi-file edits over sequential single-file changes.
- Follow `AGENTS.md` §1 Workspace Entry Protocol; handoff per §3.
- `.agentvault/` owns task routing and handoff; MemoryBank owns leases and the
  journal. Never create `.agentvault/MemoryBank/`.
